# Committed env for production / CI builds (`next build`). Values here are PUBLIC
# (NEXT_PUBLIC_* ship in the browser bundle) — do not put secrets here.

NEXT_PUBLIC_XUMM_API_KEY="77350a9a-19d6-4b99-a5e0-feca3c60be07"
