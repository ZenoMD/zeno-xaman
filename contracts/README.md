# EVM Destination Receiver Contract

Allows using the [Axelar ITS Executable service](https://docs.axelar.dev/dev/send-tokens/interchain-tokens/interchain-token-executable/) to route deposits into a RAILGUN pool
