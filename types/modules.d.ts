// snarkjs ships no type declarations; we only use groth16.{fullProve,prove,verify}.
declare module "snarkjs";

// Global CSS side-effect imports (Next handles these at build time).
declare module "*.css";
